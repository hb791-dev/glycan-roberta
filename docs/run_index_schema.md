# Run Index Schema

The run index will track training and evaluation runs for `GlycanProject2`.

Primary goals:

- record exact tokenizer and dataset inputs
- record exact checkpoint and result locations
- record continuation lineage
- make it easy for a colleague to understand what was run
